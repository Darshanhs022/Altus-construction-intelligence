# BI Dashboards

This folder contains dashboard snapshots built on top of
the semantic SQL views.

Each dashboard may span multiple images (`part1`, `part2`)
to preserve readability.

All business logic is implemented in SQL.
Dashboards are used strictly for visualization.
